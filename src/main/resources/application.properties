spring.application.name=Gestion_Restaurantes

# Configuración y conexion
spring.datasource.url=jdbc:postgresql://localhost:5432/gestion_restaurante
spring.datasource.username=postgres
spring.datasource.password=9238599
spring.datasource.driver-class-name=org.postgresql.Driver

# Configuración JPA(para creacion con ORM)
spring.jpa.hibernate.ddl-auto=update
# saber las consulta, en la consola
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
server.port=8080
